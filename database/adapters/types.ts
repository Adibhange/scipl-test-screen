/**
 * Database Adapter type definitions placeholder.
 * These interfaces will define the database operations that must be implemented
 * by the database adapters (e.g., SupabaseAdapter, PrismaMySQLAdapter) to support
 * clean switching between different databases.
 */

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface IDatabaseAdapter {
	// Future adapter methods go here
}
